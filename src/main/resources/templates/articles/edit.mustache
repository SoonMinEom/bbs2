이 페이지는 에딧 페이지입니다.
{{#article}}
    <form action="/articles/{{id}}/update" method="get">
        <input type="hidden" name="id" value="{{id}}">
        <input type="text" name="title" value="{{title}}" />
        <textarea rows="3" name="content">{{content}}</textarea>
        <input type="submit"/>
    </form>
{{/article}}