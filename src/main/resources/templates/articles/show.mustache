{{>layouts/header}}

<div class="card" style="width: 18rem;">
{{#article}}
    {{id}}
    <div class="card-body">
        <h5 class="card-title">{{title}}</h5>
        <p class="card-text">{{content}}</p>
        <a href="/articles/list" class="btn btn-primary">글목록</a>
    </div>
{{/article}}
</div>

{{>layouts/footer}}